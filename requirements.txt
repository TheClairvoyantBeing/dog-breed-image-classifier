# Core dependencies for Image Classifier Dog Breed Identification
# Use CPU-only PyTorch build (lighter download, no CUDA required)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0
pillow>=9.0.0
numpy>=1.20.0
